server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: file:///D:\MyWorkspace\DigitalMarketingPlatform\config-repo
